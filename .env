# Environment Configuration
NODE_ENV=development
PORT=3000

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/auraaq_bookstore
# For MongoDB Atlas (cloud), use:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/auraaq_bookstore

# JWT Secret (Change this to a random, secure string in production)
JWT_SECRET=your-super-secret-jwt-key-change-in-production-make-it-long-and-random

# Session Configuration
SESSION_EXPIRE_DAYS=7

# CORS Configuration (if needed for production)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000